#include<stdio.h>
void mian()
{
   char a[1500];
   int i,count=0;
   printf("enter a string");
   gets(a);
   scanf("%c",&ch);
   for(i=0;a[i]!='\0';i++)
   {
        if(a[i]==a[i])
        {
         count++;
         }
      printf("%d",count);
     }
  }   
   
